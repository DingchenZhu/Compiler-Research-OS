# Experiments

Reproducible experiment records. Every experiment should preserve metadata, commands, environment, raw results, analysis, and artifacts.

For experiments running on another Linux host, see `REMOTE_WORKFLOW.md`.
